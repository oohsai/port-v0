import React from "react";

export default function landingBlogs() {
  const truncate = (str: string, number: number) => {
    return str.length > length ? str.substring(0, length) + "..." : str;
  };
  return <div>Landing</div>;
}
